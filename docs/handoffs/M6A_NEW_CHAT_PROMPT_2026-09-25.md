# M6A implementation kickoff — after M5 closeout merge (updated 2026-09-27)

Continue the MoE_Top-k_Reproduction_Evaluation project in a **new high-reasoning implementation conversation**. Do not assume old chat context. Start with `git status --short`, `git status -sb`, `git branch --show-current`, `git log -5 --oneline`, and `git diff --check`; protect all user changes. Read `AGENTS.md`, `PROJECT.md`, `docs/IMPLEMENTATION_PLAN.md`, both team plans, `docs/BENCHMARK_VALIDATION_PLAN.md`, the M5-H2 review, [the M5→M6A handoff](M5_PROTOCOL_III_TO_M6A_HANDOFF_2026-09-25.md), and the [G3 receiver acceptance](../reviews/M5_TO_M6A_G3_RECEIVER_ACCEPTANCE_2026-09-25.md). Then verify the real code, CMake targets and relevant paper sources. Use the exact main revision after the M5 closeout merge as the M6A base; never assume a stale handoff revision is HEAD.

## M5 closeout gate

The receiving-party acceptance records **G3_ACCEPTANCE = PASS** on clean `main@9b3ce3747b1734602e3edf4c644ae1b6da52e8c1`. The M5 closeout documentation and receiver report were prepared in commit `89147bc` on `docs/m5-g3-closeout`. Start formal M6A work only after the M5 closeout PR containing that report and the current-status updates has merged. Then fetch the latest `main` and create `feat/m6a-performance-evaluation` from it; do not branch from the M5 feature or documentation branch.

The earlier M5-H2 `FAIL` and handoff blocker statements are historical findings from before F1 and G3 were closed. The accepted project boundary is the four-round signed-Q20.12 raw-score-to-original-order XOR mask entrypoint `protocol_iii_raw_score_mask_party`; the separate generic field Fselect/Fsort core remains two rounds. Preserve `AUTHOR_EXACT = NOT_PROVEN`, `EXACT_THEOREM_4_2_COST_MATCH = NO`, and the other limitations in the receiver acceptance. Do not treat those documented evidence limits as reopened M5 blockers.

## Assets and entrypoints to verify

- Shared `protocol_i_cmpagg_eval_party` / uCMP / DCF is the single ranking stack. Current `protocol_iii_grank_party` returns `logical_n` original-item-order additive rank shares in `Z_(2^rank_bits)`. AAV86 adaptive graph material and leakage need a new argument; do not call clique preprocessing adaptive.
- Frozen Protocol I three-round **C-INSTANTIATION** core: `protocol_i_parallel_shuffle_three_round_party`, tested by `moe_topk_m2_parallel_shuffle_process_e2e_test`; M2 logical communication matches Theorem 4.1, `AUTHOR_EXACT=NOT_PROVEN`. The raw-score→mask eight-round engineering test is optional EMP-dependent and absent from current `MOE_TOPK_ENABLE_EMP_OT=OFF` builds.
- Frozen Protocol III references: `agarwal_protocol_iii_modular_3round` and `moe_topk_protocol_iii_raw_score_modular_5round`, which output original-order shared masks.
- Two-round field **core controls**, not final mask APIs: `ProtocolIIITwoRoundParty::consume_round2` for single-target Fselect and `consume_round2_sort` for rank-order Fsort. Process executable: `moe_topk_m5f_two_round_process_e2e_test` (Fsort: `--sort-controller`). Both use shared CmpAgg, `F_(2^127-1)`, field DPF and input-independent offline P2. Both have two causal online rounds and P2 online silent. Input is priority-key shares plus already-field-shared encoded records with caller-enforced key consistency. Neither alone is a raw-score→original-order-mask route.
- H1 benchmark driver: `VFSS/tests/moe_topk/protocol_iii_communication_benchmark.py`; measured n=2,3,4,5,8,16,20,32,64,128 with communication logical/wire/offline bytes. It is **not** a runtime/PRG/LAN/WAN performance result. Paper unoptimized comparison ratio 1.42–1.44; exact logical delta `254n` bits. Common-mask optimization not implemented.

## M6A target

The current plan defines **both** Protocol I+AAV86 and Protocol III+AAV86 plus complete performance acceptance, not a single graph prototype. Preserve signed Q20.12 raw-score shares, score DESC/index ASC ties, and an original-order length-n secret-shared Top-K bit-mask with exactly K selected items. AAV86 has adaptive comparison graphs and an offline-only Dealer requirement. Theorem 5.1's `2r+1` pertains to the Protocol I compiler; Protocol III's `2r` is a project combination target needing its own causal/leakage proof.

Prepare a fair benchmark contract: same n/K/input and algorithm seeds, payload width, build/compiler, machine, network conditions, process isolation, warmup/repetitions and metric boundaries for both routes. The maintained V3 matrix is n=128,256 with K=2,8 and n=10³..10⁶ with K=80, all at r=2..5, with LAN/WAN and explicit outcome for every configuration. Required measurements include offline material/time, full-path online critical-path time, per-party send/receive and logical/wire communication, causal rounds, actual PRG calls, edges/vertices, graph/routing/adapter stages, correctness and failed/resource-limited cases. Use `NOT_MEASURED` until measured; one warmup and five formal repetitions, median/min/max. Do not compare Fselect with full Fsort or raw-score→mask as if they share a complete functional boundary.

## First-turn deliverable

Report: Git state and exact post-merge main revision; G3 acceptance reference; verified entrypoints and output contracts for both Protocol I and III; missing adapter and adaptive-graph dependencies; the first M6A design gate; a correctness/security/performance validation plan; and exact files proposed for the next stage. Do not modify `VFSS-baseline/`, paper sources or shared FSS core without an explicit necessity review. Do not commit/push unless the user asks. Keep M6A unstarted if the M5 closeout PR has not merged or if the adaptive exact-edge offline preprocessing and leakage contract are not reviewable.

