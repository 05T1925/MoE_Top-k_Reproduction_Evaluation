不要假设，主动暴露不确定。
只写解决当前问题的最小代码，只改必须改的地方。
信任VFSS内部代码，不为“不可能发生”的场景加防御。
禁止吞错误、禁止一次性抽象。

	
指令：
	
Avoid degradation handling, fallback, hacks, heuristics, local stabilizations, or post-processing bandages that are not faithful general algorithms.


